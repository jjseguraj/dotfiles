# Extra paths.
# This file is aimed to keep extra paths, which may be different between
# installations, in a separate file and keep only in .bashrc the common
# configuratinon  bits. This helps to share .bashrc between installations more
# easily.
export PATH=$PATH:/usr/local/x-tools/arm-unknown-linux-gnueabi/bin

